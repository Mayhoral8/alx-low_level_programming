#include "main.h"
#include <stdio.h>
/**
 * int _abs(int) - Function that computes the absolute value of an integer
 *
 * Description: blah
 *
 * Return: Always 0
 */

int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);

	}
	else 
	{
		return (n);
	}
}
