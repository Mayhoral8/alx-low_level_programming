#include "main.h"
/**
 * add - Program that adds two integers
 *
 * Description: ds
 *
 * Return: added
 */
int add(int x, int y)
{
	
	sum;
	add = x + y;
	result;
	result = sum(x, y);
	return (result);
}
