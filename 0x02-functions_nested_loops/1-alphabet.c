#include "main.h"
/**
 * main - Entry point
 *
 * Description: Alphabet again
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter = 'a';

	while (letters <= z)
		_putchar(letters);
	_putchar('\n');
		return (0);
}
	
