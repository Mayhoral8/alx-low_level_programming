#include "main.h"
/**
 * add - Program that adds two integers
 *
 * Description: ds
 *
 * Return: added
 */
int add(int x, int y)
{
	
	add;
	add = x + y;
	result;
	result = add(x, y);
	return (result);
}
