#include "main.h"
/**
 * add - Program that adds two integers
 *
 * Description: ds
 *
 * Return: added
 */
int add(int x, int y)
{
	addup = x + y;
	result;
	result = addup(x, y);
	return (result);
}
